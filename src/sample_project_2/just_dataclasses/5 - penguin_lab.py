# 5. Putting It All Together

"""Dataset: (reuse your weight_log penguins from last project)

Task:

Write a Penguin dataclass with species, sex, body_mass, flipper_length.

Add a property mass_to_flipper_length_ratio.

Create a list of penguins.

Print:

The first 5 ratios.

All penguins with species "Chinstrap", showing sex:ratio.

The average ratio across all penguins.

Hint: Use a list comprehension for step 1, an if filter for step 2, and sum(...) / len(...) for step 3."""