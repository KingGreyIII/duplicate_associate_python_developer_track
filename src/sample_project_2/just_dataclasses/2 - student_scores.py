students = [
    ("Alice", [78, 82, 91]),
    ("Bob", [65, 70, 72]),
    ("Charlie", [90, 95, 88]),
]
"""Task:

Create a Student dataclass with name and scores.

Add a property average_score that calculates the mean of scores.

Loop through and print each student’s name with their average score.

Hint: Use sum(self.scores) / len(self.scores) in the property."""